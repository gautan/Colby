@startuml
  title Application Flow (from IMG_3412)

  start
  :Clients;

  :Receives Research;

  if (In CRM?) then (Yes)
    :Receives registration page;
    :Receives cookie email;
    :Views research report;
    stop
  else (No)
    :Receives registration page;
    :Receives failure email;

    :Try alternate email\nfound in CRM;
    if (Alternate email works?) then (Yes)
      :Receives cookie email;
      :Views research report;
      stop
    else (No)
      :Contact salesperson\nor BL sales team;
      :Fills & submits\nregistration form;

      note right
        - Sales contact receives the registration form
        - Requested to set up client in CRM
        - Copy of registration form sent to support team
      end note

      :Receives temporary\ncookie email;
      :Views research report;
      stop
    endif
  endif
  @enduml
